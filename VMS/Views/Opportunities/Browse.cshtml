@model IEnumerable<VMS.Models.Opportunity>
@{
    ViewData["Title"] = "Browse Opportunities";
}
<h2>View Posts</h2>
<ol class="breadcrumb">
    <li class="breadcrumb-item"><a  asp-area="" asp-controller="Home" asp-action="Index">Home</a></li>
    <li class="breadcrumb-item active">Browse</li>
</ol>
<div class="row">
    <!--left-->
    <div class="col col-md-3">
        <button type="button" class="btn btn-outline-primary btn-block ml-3 dropdown-toggle" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
            Filter
        </button>
        <div class="dropdown-menu">
            @*<a type="button" class="dropdown-item" asp-action="">A-Z</a>*@
            <a type="button" class="dropdown-item" asp-action="BrowseReverseAlphabetical">Z-A</a>
            @*<a type="button" class="dropdown-item" asp-action="">No signups</a>*@
            <a type="button" class="dropdown-item" asp-action="BrowseRecent">Most Recent</a>
            <div class="dropdown-divider"></div>
            <a type="button" class="dropdown-item" asp-action="Browse">All</a>
        </div>

        <form class="form-group pt-3" asp-action="ShowSearchResults">
            <div class="form-row">
                <label class="sr-only" for="SearchPhrase"></label>
                <input name="SearchPhrase" class="rounded form-control" id="inlineFormInputName" placeholder="Search">
            </div>
        </form>
    </div>

    <!--right-->
    <div class="col col-md-9">
        @foreach (var item in Model)
        {
            <div class="card rounded mb-5 p-2">
                <div class="card-body">
                    <h5>@Html.DisplayFor(modelItem => item.OpportunityName)</h5><hr />
                    <p class="font-weight-light">@Html.DisplayFor(modelItem => item.City), @Html.DisplayFor(modelItem => item.State)</p>
                    <p>@Html.DisplayFor(modelItem => item.Description)</p>
                    <!-- Button trigger modal -->
                    <button type="button" class="btn btn-primary" data-toggle="modal" data-target="#applyModal">
                        Apply
                    </button>

                    <!-- Modal -->
                    <div class="modal fade" id="applyModal" tabindex="-1" role="dialog" aria-labelledby="applyModalLabel" aria-hidden="true">
                        <div class="modal-dialog" role="document">
                            <div class="modal-content">
                                <div class="modal-header">
                                    <h5 class="modal-title" id="exampleModalLabel">Would you like to apply for...</h5>
                                    <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                                        <span aria-hidden="true">&times;</span>
                                    </button>
                                </div>
                                <div class="modal-body">
                                    <h5>@Html.DisplayFor(modelItem => item.OpportunityName)</h5>
                                    <h6>Start: @Html.DisplayFor(modelItem => item.StartDate)</h6>
                                    <h6>End: @Html.DisplayFor(modelItem => item.EndDate)</h6>
                                </div>
                                <div class="modal-footer">
                                    <button type="button" class="btn btn-secondary" data-dismiss="modal">Close</button>
                                    <a class="btn btn-primary" asp-area="" asp-controller="Opportunities" asp-action="Apply">Apply</a>
                                </div>
                            </div>
                        </div>
                    </div>
                    <a class="btn btn-outline-info btn-rounded" asp-area="" asp-controller="Opportunities" asp-action="PublicDetails" asp-route-id="@item.Id">See Details</a>
                </div>
            </div>
        }
    </div>
</div>


@*<div class="container">

        <div class="row-cols-md-6">

            <button type="button" class="btn btn-outline-primary ml-3 dropdown-toggle" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                Filter
            </button>
            <div class="dropdown-menu">
                <a class="dropdown-item" asp-action="">Most Recent</a>
                <div class="dropdown-divider"></div>
                <a class="dropdown-item" asp-action="Browse">All</a>
            </div>


            <form class="form-group mb-5" asp-action="ShowSearchResults">
                <div class="form-row">
                    <label class="sr-only" for="SearchPhrase"></label>
                    <input name="SearchPhrase" class="rounded form-control" id="inlineFormInputName" placeholder="Search">
                </div>
            </form>
        </div>


        @foreach (var item in Model)
        {
            <div class="card rounded m-5">
                <div class="card-body">
                    <h5>@Html.DisplayFor(modelItem => item.opportunityName)</h5><hr />
                    <p class="font-weight-light">@Html.DisplayFor(modelItem => item.location)</p>
                    <p>@Html.DisplayFor(modelItem => item.description)</p>
                    <a class="btn btn-outline-primary btn-rounded" asp-area="" asp-controller="Opportunities" asp-action="Apply">Apply</a>
                    <a class="btn btn-outline-info btn-rounded" asp-action="Details" asp-route-id="@item.Id">Details</a>
                </div>
            </div>
        }
        <a class="btn btn-lg btn-secondary btn-rounded justify-content-lg-center" asp-area="" asp-controller="Home" asp-action="Index">Home</a>
    </div>*@
